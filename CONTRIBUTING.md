Contributing

Thank you for your interest. For this submission repo, please follow these simple rules:

- Create feature branches named `feat/short-description`.
- Keep commits small and focused with meaningful messages.
- Run `npm install` and `npm run dev` locally to verify functionality before opening a PR.
- Files changed should preserve existing code style.

